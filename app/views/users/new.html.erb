
<!DOCTYPE html>
<html>
  <head>
    <title>Mock</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= render 'layouts/rails_default' %>
  </head>
  <body>
    <div class="new-body">
      <%= link_to root_path, class: "new-top-title" do %>
        <h1 class="new-h1"><span class="new-title">mock</span></h1>
      <% end %>
      <div class="container form-container">
        <div class="row">
          <div class="col-xs-10 col-xs-offset-1">
            <h2 class="sign-message">mockへようこそ！</h2>
            <h3 class="sign-message">会員登録(無料)をして、感動をシェアしましょう</h3>
            <p class="sign-message">※mockアカウントを持っている場合は<%= link_to "こちら", signin_path %>から</p>
            <hr>
            <%= render 'form', user: @user %>
          </div>
        </div>
      </div>
    </div>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
