<%= link_to hack do  %>
  <section class="hack-content">
    <div class="hack-text">
      <div class="names">
        <div class="category-name">
          <%= hack.category.name %>
        </div>
        <div class="role-name">
          -<%= hack.role.name %>-
        </div>
      </div>
      <% begin %>
        <div class="pointing-up"><i class="fas fa-level-up-alt"></i> <%= point %></div>
      <% rescue %>
      <% end %>
      <label style="display: block;">
        <div class="tag-lists">
          <% hack.tag_list.each do |tag| %>
            <span class="tag-list"><%= tag %></span>
          <% end %>
        </div>
      </label>
      <div class="overview">
        <%= hack.overview.truncate(100)%>
      </div>
    </div>
  </section>
<% end %>