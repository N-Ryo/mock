<%= form_with model: current_user.user_category_relationships.build do |f| %>
  <div><%= hidden_field_tag :hack_id, hack.id %></div>
  <%= button_tag class: "btn fab fa-lg fa-telegram-plane category-follow", type: 'submit' do %>
    <%= hack.category.users.count%>
  <% end %>
<% end %>